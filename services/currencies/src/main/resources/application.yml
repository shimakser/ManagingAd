server:
  port: 8081

feign:
  currencies-url: ${CURRENCIES_URL}

spring:
  data:
    mongodb:
      host: ${MONGO_HOST}
      port: ${MONGO_PORT}
      database: ${DB}

  rabbitmq:
    host: {RABBITMQ_HOST}
    port: {RABBITMQ_PORT}
    username: {RABBITMQ_USERNAME}
    password: {RABBITMQ_PASSWORD}
